.venv
venv
# Ignore only the runtime incident database at the project root.  `data` without
# a leading slash also excludes `sandbox/data`, which contains the scenario data
# needed by the serverless app.
/data/
eval
tests
docs
.claude
.git
*.db
.env
Dockerfile
docker-compose.yml
Makefile
live/*.py
