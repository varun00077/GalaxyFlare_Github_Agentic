"""SOCrates sandbox: a synthetic enterprise environment (NIDS alerts, flows, assets,
CVE KB, host logs, firewall, tickets) exposed as an HTTP service.

Everything here is fake and deterministic. Nothing touches a real network.
"""
